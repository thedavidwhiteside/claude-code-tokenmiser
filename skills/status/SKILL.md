---
description: Show today's Claude Code token usage and quota status
disable-model-invocation: true
---

Run this command and show the output to the user exactly as-is, with no extra commentary:

```bash
python3 ${CLAUDE_PLUGIN_ROOT}/hooks/quota_status.py
```
